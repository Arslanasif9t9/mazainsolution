# Mazain Solution — Frontend (React)


## Setup

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` and set `VITE_API_BASE_URL` to your backend's API URL
(defaults to `http://localhost:5000/api`).
